# The modules were taken from FORGE - no changes!

* For modules `cmd_opts` - arg - `hypernetworks` - was added to avoid problems with extensions.


> MODULES THAT HAVE BEEN CHANGED ARE MARKED WITH THE COMMENT `# ANXIETY`.